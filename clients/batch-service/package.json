{
  "name": "batch-service",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node src/index.js",
    "lint": "eslint .",
    "unit-test": "echo 'No batch-service tests configured yet'",
    "test": "npm run lint && npm run unit-test",
    "e2e:test": "jest --runInBand tests/e2e.test.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "prom-client": "^14.0.0"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "jest": "^29.7.0",
    "supertest": "^6.4.2"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": ["**/tests/**/*.test.js"]
  }
}